[build]
  command = "node -e \"console.log('Using prebuilt PureInvest OS deployment artifact')\""
  publish = "dist"
  functions = "netlify/functions"

[[redirects]]
  from = "/app"
  to = "/app.html"
  status = 200
